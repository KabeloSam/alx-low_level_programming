#include <stdio.h>
/**
 * main -entry point
 * 
 * return always 0(success) function that prints programming is like building a multingual puzzle
 */
int main(void) 
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
